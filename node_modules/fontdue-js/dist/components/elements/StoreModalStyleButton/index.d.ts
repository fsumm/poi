import React from 'react';
import { ButtonProps } from '../Button';
type StoreModalStyleButtonProps = Omit<ButtonProps, 'children'> & {
    selected: boolean;
    children: {
        name: React.ReactNode;
        featureGlyphs: React.ReactNode;
        price: React.ReactNode;
    };
};
declare const StoreModalStyleButton: ({ selected, children: { name, featureGlyphs, price }, ...rest }: StoreModalStyleButtonProps) => React.JSX.Element;
export default StoreModalStyleButton;
