import React from 'react';
interface StoreModalPageContainerProps {
    children: {
        body: React.ReactNode;
        stickyFooter?: React.ReactNode;
    };
}
declare const StoreModalPageContainer: ({ children: { body, stickyFooter }, }: StoreModalPageContainerProps) => React.JSX.Element;
export default StoreModalPageContainer;
