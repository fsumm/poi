import React from 'react';
import { Price_price$key } from '__generated__/Price_price.graphql';
export declare enum PriceFormat {
    Default = "default",
    Truncated = "truncated"
}
type Price_baseProps = {
    format?: PriceFormat;
    invert?: boolean;
    subtractFrom?: {
        amount: number;
    };
    multiplier?: number;
    isLoading?: boolean;
};
export type Price_props = Price_baseProps & {
    price: {
        amount: number;
        currency: string;
    } | null | undefined;
};
export declare function Price({ price, format, invert, // multiply by -1
subtractFrom, // amount to subtract from
multiplier, // multiply the result amount?
isLoading, ...rest }: Price_props): React.JSX.Element;
type RelayPrice_props = Price_baseProps & {
    price: Price_price$key | null;
};
declare function RelayPrice({ price: priceKey, ...rest }: RelayPrice_props): React.JSX.Element;
export default RelayPrice;
