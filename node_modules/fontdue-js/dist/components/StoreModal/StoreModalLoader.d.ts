import React from 'react';
interface StoreModalLoader_props {
    children: React.ReactNode;
}
declare function StoreModalLoader({ children }: StoreModalLoader_props): React.JSX.Element | null;
export default StoreModalLoader;
