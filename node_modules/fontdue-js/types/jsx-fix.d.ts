import { JSX as ReactJSX } from 'react';

declare global {
  namespace JSX {
    // fix for react-stripe-js which still expects JSX to be a global
    interface Element extends ReactJSX.Element {}
  }
}
